# Select discoveries, not materials

Each Daily Edition Selection Slot selects an underlying Discovery and then recommends one Material through which to encounter it. Selecting source artifacts directly would let repeated coverage compete as separate choices and would confuse a subject's significance with the accessibility of a particular source; source provenance and alternatives remain available internally.
